namespace = planet_setup

#pc_hive_medium
#class = "pc_hive_medium"
#flags = { hiveplanet_planet }
event = {
    id = planet_setup.1
    hide_window = yes
    fire_only_once = yes
    
	immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_medium }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			while = {
			   count = 2
			   add_building = energy_factory
			}
			while = {
			   count = 2
			   add_building = building_foundry_1
			}
			while = {
				count = 20
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_civilian_industry
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			while = {
				count = 50
				create_pop = {
					species = owner_main_species
				}
			}
		}
	}
}


#pc_forge
#class = "pc_forge"		
#flags = { forgeplanet_planet }
event = {
    id = planet_setup.2
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_forge }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = mineral_factory
			while = {
			   count = 3
			   add_building = building_foundry_1
			}
			while = {
			   count = 10
			   add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
			   count = 5
			   add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
			   count = 10
			   add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_forgee
#class = "pc_forgee"		
#flags = { forgeeplanet_planet }
event = {
    id = planet_setup.3
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_forgee }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = mineral_factory    
			while = {
				count = 4
				add_building = building_foundry_1
			}
			while = {
			   count = 10
			   add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
			   count = 5
			   add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
			   count = 10
			   add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 40
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_forgeee
#class = "pc_forgeee"		
#flags = { forgeeeplanet_planet }
event = {
    id = planet_setup.4
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_forgeee }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = mineral_factory   
			while = {
				count = 5
				add_building = building_foundry_1
			}
			while = {
			   count = 10
			   add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
			   count = 5
			   add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
			   count = 10
			   add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 45
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_industrial
#class = "pc_industrial"
#flags = { industrialplanet_planet }
event = {
    id = planet_setup.5
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_industrial }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory    
			while = {
				count = 4
				add_building = building_factory_1
			}
			while = {
				count = 13
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 7
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_agri
#class = "pc_agri"
#flags = { agriplanet_planet }
event = {
    id = planet_setup.6
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_agri }
			add_modifier = { modifier = "agri_planet" days = -1 }
			add_building = building_system_capital
			add_building = food_factory
			add_building = food_factory
			while = {
				count = 25
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 25
				create_pop = { species = owner_main_species }
			}
		}
	}
}

#pc_shrine
#class = "pc_shrine"
#flags = { shrineplanet_planet }
event = {
    id = planet_setup.7
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_shrine }
			add_modifier = { modifier = "shrine_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			while = {
			   count = 3
			   add_building = building_citadel_of_faith
			}
			while = {
			   count = 17
			   add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
			   count = 6
			   add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
			   count = 3
			   add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 20
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_penal
#class = "pc_penal"
#flags = { penalplanet_planet }
event = {
    id = planet_setup.8
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_penal }
			add_modifier = { modifier = "penal_planet" days = -1 }
			add_building = building_system_capital
			add_building = mineral_factory  
			add_building = mineral_factory
			while = {
			   count = 11
			   add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
			   count = 5
			   add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 9
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 20
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_fortress
#class = "pc_fortress"
#flags = { fortressplanet_planet }
event = {
    id = planet_setup.9
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_fortress }
			add_modifier = { modifier = "fortress_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory    
			while = {
			   count = 11
			   add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
			   count = 7
			   add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
			   count = 7
			   add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
			   count = 4
			   add_building = building_stronghold
			}
			while = {
				count = 30
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_tomb
#class = "pc_tomb"
#flags = { tombplanet_planet }
event = {
    id = planet_setup.10
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_tomb }
			add_modifier = { modifier = "tomb_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory    
			add_building = building_robot_assembly_plant
			add_building = building_machine_assembly_plant
			while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_nexus
				}
			}
			while = {
				count = 15
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator_uncapped
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining_uncapped
				}
			}
			while = {
				count = 70
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_hive_arid
#class = "pc_hive_arid"
#flags = { hive_aridplanet_planet }
event = {
    id = planet_setup.11
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_arid }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory  
			while = {
				count = 4
				add_building = building_factory_1
			}
			while = {
				count = 17
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_arms_industry
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_hive_continental_01
#class = "pc_hive_continental_01"
#flags = { hive_continental_01planet_planet }
event = {
    id = planet_setup.12
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_continental_01 }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = energy_factory
			while = {
				count = 4
				add_building = building_factory_1
			}
			while = {
				count = 20
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_arms_industry
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_hive_continental_02
#class = "pc_hive_continental_02"
#flags = { hive_continental_02planet_planet }
event = {
    id = planet_setup.13
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_continental_02 }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = energy_factory
			}
			while = {
				count = 4
				add_building = building_factory_1
			}
			while = {
				count = 15
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 8
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_civilian_industry
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_hive_ocean
#class = "pc_hive_ocean"
#flags = { hive_oceanplanet_planet }
event = {
    id = planet_setup.14
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_ocean }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = energy_factory
			}
			while = {
				count = 4
				add_building = building_factory_1
			}
			while = {
				count = 14
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 9
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_civilian_industry
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_hive_tundra
#class = "pc_hive_tundra"
#flags = { hive_tundraplanet_planet }
event = {
    id = planet_setup.15
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_tundra }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = energy_factory		
			while = {
				count = 4
				add_building = building_factory_1
			}
			while = {
				count = 14
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 8
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_arms_industry
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			while = {
				count = 35
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_super_hive
#class = "pc_super_hive"
#flags = { super_hiveplanet_planet }
event = {
    id = planet_setup.16
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_super_hive }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = energy_factory
			}
			while = {
				count = 5
				add_building = building_factory_1
			}
			while = {
				count = 15
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 8
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_civilian_industry
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}
			while = {
				count = 70
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_mega_hive
#class = "pc_mega_hive"
#flags = { mega_hiveplanet_planet }
event = {
    id = planet_setup.17
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_mega_hive }
			add_modifier = { modifier = "hive_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = energy_factory
			}	
			while = {
				count = 5
				add_building = building_factory_1
			}
			while = {
				count = 15
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 8
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_civilian_industry
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_leisure
				}
			}	
			while = {
				count = 100
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_forge_ring
#class = "pc_forge_ring"
#flags = { forge_ringplanet_planet }
event = {
    id = planet_setup.18
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_forge_ring }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			while = {
				count = 2
				add_building = mineral_factory
			}	
			while = {
				count = 5
				add_building = building_factory_1
			}
			while = {
				count = 10
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_city
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_science
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_nexus
				}
			}
			while = {
				count = 45
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_forge_ring2
#class = "pc_forge_ring2"

event = {
    id = planet_setup.19
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_forge_ring2 }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = mineral_factory
			}		
			add_building = energy_factory
			while = {
				count = 5
				add_building = building_factory_1
			}
			while = {
				count = 12
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_city
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_science
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_commercial
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_forge_ring3
#class = "pc_forge_ring3"

event = {
    id = planet_setup.20
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_forge_ring3 }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory    
			while = {
				count = 2
				add_building = mineral_factory
			}	
			while = {
				count = 5
				add_building = building_foundry_1
			}
			while = {
				count = 12
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_city
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_rw_science
				}
			}
			add_district_and_planet_size_if_needed_effect = {
				district = district_rw_commercial
			}
			while = {
				count = 55
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_craftworld
#class = "pc_craftworld"

event = {
    id = planet_setup.21
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_craftworld }
			add_modifier = { modifier = "craftworld_planet" days = -1 }
			add_building = building_hab_major_capital
			add_building = building_mineral_purification_hub
			while = {
				count = 2
				add_building = building_class_4_singularity
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 12
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_housing
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_energy
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_science
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_commercial
				}
			}
			while = {
				count = 70
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_hive_industrial
#class = "pc_hive_industrial"

event = {
    id = planet_setup.22
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_hive_industrial }
			add_modifier = { modifier = "forge_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory  
			while = {
				count = 5
				add_building = building_factory_1
			}
			while = {
				count = 11
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_housing
				}
			}
			while = {
				count = 9
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_arms_industry
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_arcology_civilian_industry
				}
			}
			while = {
				count = 55
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_advancecity
#class = "pc_advancecity"

event = {
    id = planet_setup.23
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_advancecity }
			add_modifier = { modifier = "advancecity_planet" days = -1 }
			add_building = building_hive_capital
			while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 15
				add_district_and_planet_size_if_needed_effect = {
					district = district_hive
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_nexus
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_webway
#class = "pc_webway"

event = {
    id = planet_setup.24
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_webway }
			add_modifier = { modifier = "webway_planet" days = -1 }
			add_building = building_hab_major_capital
			while = {
				count = 12
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_housing
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_energy
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_science
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_commercial
				}
			}
			while = {
				count = 70
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_spacehulk
#class = "pc_spacehulk"
#flags = { hiveplanet_planet }
event = {
    id = planet_setup.25
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_spacehulk }
			add_modifier = { modifier = "spacehulk_planet" days = -1 }
			add_building = building_hab_major_capital
			while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 16
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_housing
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_energy
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_science
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_commercial
				}
			}
			while = {
				count = 20
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_blackstonefortress
#class = "pc_blackstonefortress"
#flags = { hiveplanet_planet }
event = {
    id = planet_setup.26
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_blackstonefortress }
			add_modifier = { modifier = "blackstonefortress_planet" days = -1 }
			add_building = building_hab_major_capital
			add_building = energy_factory
			add_building = mineral_factory    
			while = {
				count = 21
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_housing
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_energy
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_science
				}
			}
			while = {
				count = 10
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_caliban
#class = "pc_caliban"

event = {
    id = planet_setup.27
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_caliban }
			add_modifier = { modifier = "caliban_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory   
			while = {
				count = 18
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 10
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_eternalcrusader
#class = "pc_eternalcrusader"

event = {
    id = planet_setup.28
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_eternalcrusader }
			add_modifier = { modifier = "eternalcrusader_planet" days = -1 }
			add_building = building_hab_major_capital
			add_building = energy_factory
			add_building = mineral_factory    
			while = {
				count = 21
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_housing
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_energy
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_hab_science
				}
			}
			while = {
				count = 20
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_daemon
#class = "pc_daemon"

event = {
    id = planet_setup.29
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_daemon }
			add_modifier = { modifier = "daemon_planet" days = -1 }
			add_building = building_system_capital
            while = {
			   count = 2
			   add_building = building_factory_1
			}
			while = {
			   count = 2
			   add_building = building_foundry_1
			}
			while = {
				count = 13
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_daemonn
#class = "pc_daemonn"

event = {
    id = planet_setup.30
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_daemonn }
			add_modifier = { modifier = "daemonn_planet" days = -1 }
			add_building = building_system_capital
            while = {
			   count = 2
			   add_building = building_factory_1
			}
			while = {
			   count = 2
			   add_building = building_foundry_1
			}
			while = {
				count = 13
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_daemont
#class = "pc_daemont"

event = {
    id = planet_setup.31
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_daemont }
			add_modifier = { modifier = "daemont_planet" days = -1 }
			add_building = building_system_capital
            while = {
			   count = 2
			   add_building = building_factory_1
			}
			while = {
			   count = 2
			   add_building = building_foundry_1
			}
			while = {
				count = 13
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_daemons
#class = "pc_daemons"

event = {
    id = planet_setup.32
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_daemons }
			add_modifier = { modifier = "daemons_planet" days = -1 }
			add_building = building_system_capital
            while = {
			   count = 2
			   add_building = building_factory_1
			}
			while = {
			   count = 2
			   add_building = building_foundry_1
			}
			while = {
				count = 13
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 6
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_advancexenocity
#class = "pc_advancexenocity"

event = {
    id = planet_setup.33
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_advancexenocity }
			add_modifier = { modifier = "advancexenocity_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 16
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 50
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_advancexenocityy
#class = "pc_advancexenocityy"

event = {
    id = planet_setup.34
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_advancexenocityy }
			add_modifier = { modifier = "advancexenocityy_planet" days = -1 }
			add_building = building_system_capital
            while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 16
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 55
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_advancexenocityyy
#class = "pc_advancexenocityyy"

event = {
    id = planet_setup.35
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_advancexenocityyy }
			add_modifier = { modifier = "advancexenocityyy_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 13
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 5
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 70
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_orkworld
#class = "pc_orkworld"

event = {
    id = planet_setup.36
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_orkworld }
			add_modifier = { modifier = "orkworld_planet" days = -1 }
			add_building = building_crude_huts
			add_building = building_primitive_dwellings
			add_building = building_stone_palace
			add_building = building_primitive_factory
			add_building = building_primitive_mine
			add_building = building_primitive_power_plant
			add_building = building_primitive_farm
			add_building = building_urban_dwellings
			while = {
				count = 2
				add_building = building_factory_1
			}
			while = {
				count = 2
				add_building = building_foundry_1
			}
			while = {
				count = 17
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 100
				create_pop = { species = owner_main_species }
			}
		}
    }
}


#pc_armoury
#class = "pc_armoury"

event = {
    id = planet_setup.37
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_armoury }
			add_modifier = { modifier = "armoury_planet" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = armoury_imperium
			while = {
				count = 14
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 7
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 20
				create_pop = { species = owner_main_species }
			}

		}
    }
}


#pc_destroyedplanet
#class = "pc_destroyedplanet"

event = {
    id = planet_setup.38
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_destroyedplanet }
			add_modifier = { modifier = "destroyed_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 19
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 4
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 5
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_arid
#pc_savannah
#pc_tropical
#pc_continental
#pc_ocean
#pc_tundra
#pc_arctic
#pc_alpine

#class = "pc_desert"
#class = "pc_tropical"
#class = "pc_arid"
#class = "pc_ocean"
#class = "pc_tundra"
#class = "pc_arctic"
#class = "pc_continental"
#class = "pc_alpine"
#class = "pc_savannah"

event = {
    id = planet_setup.39
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { 
				has_owner = yes
				or = {
					is_planet_class = pc_desert
					is_planet_class = pc_tropical
					is_planet_class = pc_arid
					is_planet_class = pc_ocean
					is_planet_class = pc_tundra
					is_planet_class = pc_arctic
					is_planet_class = pc_continental
					is_planet_class = pc_alpine
					is_planet_class = pc_savannah
				}
			}
			add_building = building_system_capital
			add_building = energy_factory
			while = {
				count = 19
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 10
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_nuked ?????
#class = "pc_nuked"

event = {
    id = planet_setup.40
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_nuked }
			add_modifier = { modifier = "nuked_planet" days = -1 }
			add_building = building_system_capital
			while = {
				count = 18
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			add_district_and_planet_size_if_needed_effect = {
				district = district_farming
			}
			while = {
				count = 15
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#pc_gaia
#class = "pc_gaia"

event = {
    id = planet_setup.41
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
		every_planet = {
			limit = { has_owner = yes is_planet_class = pc_gaia }
			add_modifier = { modifier = "gaia_world" days = -1 }
			add_building = building_system_capital
			add_building = energy_factory
			add_building = mineral_factory
			while = {
				count = 18
				add_district_and_planet_size_if_needed_effect = {
					district = district_city
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_generator
				}
			}
			while = {
				count = 2
				add_district_and_planet_size_if_needed_effect = {
					district = district_mining
				}
			}
			while = {
				count = 3
				add_district_and_planet_size_if_needed_effect = {
					district = district_farming
				}
			}
			while = {
				count = 10
				create_pop = { species = owner_main_species }
			}
		}
    }
}

#spacemarinehomeworld

event = {
    id = planet_setup.42
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
			every_planet = {
			limit = { has_owner = yes has_planet_flag = spacemarinehomeworld }
			add_modifier = { modifier = "spacemarine_planet" days = -1 }
			add_building = building_citadel_of_faith
		}
    }
}

#mechanicushomeworld

event = {
    id = planet_setup.43
    hide_window = yes
    fire_only_once = yes
    
    immediate = {
			every_planet = {
			limit = { has_owner = yes has_planet_flag = mechanicushomeworld }
			add_modifier = { modifier = "mechanicus_planet" days = -1 }
			add_building = building_supercomputer
		}
    }
}