namespace = actions

country_event = {
	id = actions.1
	hide_window = yes

	is_triggered_only = yes

	immediate = {
		set_timed_country_flag = {
			flag = asked_to_leave_confederation
			days = 3600
		}
		add_opinion_modifier = {
			who = from
			modifier = opinion_denied_to_leave_confederation
		}
		from = {
			add_opinion_modifier = {
				who = root
				modifier = opinion_wanted_to_leave_confederation
			}
		}
	}
}