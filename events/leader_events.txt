namespace = leaders

country_event = {
    id = leaders.1
	hide_window = yes
    fire_only_once = yes

	immediate = {
		every_owned_leader = {
			limit = { leader_class = ruler }
			add_trait = leader_trait_ruler_chosen
		}
	}
}